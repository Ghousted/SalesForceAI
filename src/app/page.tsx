import { LandingPage } from "@/components/LandingPage";

export const metadata = {
  title: "Sales OS — AI owns the system, the human owns the close",
  description:
    "An agent-native CRM. A roster of named AI teammates prospects, qualifies, reconciles and follows up — automatically. Every send waits for your approval.",
};

export default function Home() {
  return <LandingPage />;
}
