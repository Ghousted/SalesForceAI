# Sales OS — environment

# LLM provider is intentionally undecided for now (PRD: "decide later").
# The app runs fully on a stubbed provider. To wire a real one later, set:
# LLM_PROVIDER=anthropic|openai|stub   (default: stub)
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# CRM (Phase 2 — live HubSpot integration). Phase 1 runs on synthetic data only.
# HUBSPOT_ACCESS_TOKEN=

LLM_PROVIDER=stub
